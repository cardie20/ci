#!groovy


node ( neptuno ) {

   
            stage('Code | Checkout') {
                echo "${env.GIT_BRANCH}"
                echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
   
            }

}
